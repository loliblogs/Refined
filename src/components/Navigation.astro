---
import { FilesIcon, ListIcon } from '@phosphor-icons/react';

import { getSiteConfig } from '@/config/site.config';
import type { CollectionName } from '@/types/content';
import { getBasePath, getFullPath, isRootPath } from '@/utils/collection-paths';

import NavigationController from './NavigationController';

interface Props {
  collection?: CollectionName | undefined;
}

const { collection = 'post' } = Astro.props;
const config = getSiteConfig(collection);
const homePath = getBasePath(collection);
const currentPath = Astro.url.pathname;

// 静态计算导航项是否激活
function isNavActive(path: string, recursive: boolean): boolean {
  if (recursive) {
    return currentPath === path
      || (!isRootPath(path) && currentPath.startsWith(`${path}/`));
  }
  return currentPath === path;
}
---

<nav
  id="nav"
  class={`
    relative h-full w-nav shrink-0 bg-nav
    max-lg:fixed max-lg:z-10 max-lg:h-auto max-lg:w-full
    max-lg:peer-checked/menu:z-10
  `}
>
  {/* 移动端顶部栏 */}
  <div
    id="nav-mobile-bar"
    class={`
      hidden h-11 items-center justify-center bg-nav
      max-lg:flex
    `}
  >
    <label
      for="sidebar-toggle"
      class={`
        m-0 flex size-11 shrink-0 cursor-pointer items-center justify-center
        bg-transparent text-primary transition-colors duration-100 ease-in-out
        hover:bg-active
      `}
      aria-label="打开侧边栏"
    >
      <FilesIcon size="1.25rem" weight="regular" />
    </label>
    <a
      href={homePath}
      class={`
        m-0 flex h-11 min-w-0 flex-1 items-center justify-center truncate
        text-center text-2xl font-bold text-primary no-underline
        transition-colors duration-100 ease-in-out
        hover:text-secondary
      `}
    >
      {config.title}
    </a>
    <label
      for="menu-toggle"
      class={`
        m-0 flex size-11 shrink-0 cursor-pointer items-center justify-center
        bg-transparent text-primary transition-colors duration-100 ease-in-out
        hover:bg-active
      `}
      aria-label="切换菜单"
    >
      <ListIcon size="1.25rem" weight="regular" />
    </label>
  </div>

  {/*
    * 菜单项
    * 任意值语法：Tailwind 不支持 group-peer-checked/{name} 命名变体，
    * 需要用 [#menu-toggle:checked~#nav_&] 精确匹配 checkbox 状态
    */}
  {/* menu labels 已在配置层预处理，直接使用 set:html */}
  <div
    id="nav-menu"
    class={`
      flex h-full flex-col justify-center
      max-lg:h-0 max-lg:overflow-hidden max-lg:opacity-0
      max-lg:transition-[height_0.35s_cubic-bezier(0.4,0,0.2,1),opacity_0.25s_ease-out]
      max-lg:[#menu-toggle:checked~#nav_&]:h-11
      max-lg:[#menu-toggle:checked~#nav_&]:opacity-100
      max-lg:[#menu-toggle:checked~#nav_&]:transition-[height_0.35s_cubic-bezier(0.4,0,0.2,1),opacity_0.25s_ease-in_0.1s]
    `}
  >
    <div
      class={`
        flex max-h-full flex-col overflow-y-auto
        max-lg:block max-lg:overflow-x-auto max-lg:overflow-y-hidden
        max-lg:text-center max-lg:whitespace-nowrap
      `}
      data-overlayscrollbars-initialize
      data-nav-menu-scroll
    >
      {Object.entries(config.menu).map(([label, navItem]) => {
        const relativePath = typeof navItem === 'object' ? navItem.path : navItem;
        const path = getFullPath(relativePath);
        const recursive = typeof navItem !== 'object' || navItem.recursive !== false;

        return (
          <a
            class:list={[
              `
                flex h-nav-height shrink-0 items-center justify-center
                text-primary no-underline transition-colors duration-100
                ease-in-out
                hover:bg-active
                max-lg:inline-block max-lg:h-11 max-lg:px-5 max-lg:align-top
                max-lg:text-base/11
              `,
              { 'bg-active': isNavActive(path, recursive) },
            ]}
            href={path}
          >
            <span
              class={`
                inline-block truncate
                max-lg:inline max-lg:text-inherit
              `}
              set:html={label}
            />
          </a>
        );
      })}
    </div>
  </div>
</nav>

<NavigationController client:idle />
